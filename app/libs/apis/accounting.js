import { db } from '@/app/libs/utils/firebase'

const accounting = {
  GetAccount: async (uid) => {

  },
  GetAccounts: async () => {

  },
  GetAccountByDay: async (day) => {

  },
  GetAccountByMonth: async (month) => {

  },
  PostAccount: async (uid, account) => {

  }
}

export default accounting
